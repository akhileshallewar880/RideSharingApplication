# Flutter Native Splash Configuration for VanYatra
# Run: flutter pub run flutter_native_splash:create
# Using minimal native splash to prevent double splash screens

flutter_native_splash:
  # Match home screen gradient start color
  color: "#1B5E20"
  
  # Disable for all platforms to show Flutter splash only
  android: false
  ios: false
  web: false
